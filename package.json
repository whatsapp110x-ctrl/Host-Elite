{
  "name": "rest-express",
  "version": "1.0.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "NODE_ENV=development tsx server/index.ts",
    "build": "NODE_OPTIONS='--max-old-space-size=1024' vite build && NODE_OPTIONS='--max-old-space-size=512' esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist --out-extension:.js=.js",
    "start": "NODE_OPTIONS='--max-old-space-size=512' NODE_ENV=production node dist/index.js",
    "check": "tsc",
    "db:push": "drizzle-kit push"
  }
}
